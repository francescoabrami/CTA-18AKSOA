\documentclass[../main.tex]{subfiles}

\graphicspath{{\subfix{../images/}}}

\begin{document}

\clearpage
\thispagestyle{empty}
\vspace*{7cm}
\section{Esercitazioni di laboratorio}
\vspace*{1.5cm}

Procediamo ora presentando le seguenti esercitazioni di laboratorio svolte durante l'intera durata del corso. Le esercitazioni qui presentate (I - VIII) sono quelle proposte durante l'anno accademico 2025 - 2026 e potrebbero non essere aggiornata rispetto agli anni successivi. Infine invitiamo il lettore a verificare personalmente le soluzioni proposte in quanto potrebbero essere non completamente corrette e non essere presentate nella loro interezza. Tutte le soluzioni proposte sono riportate subito di seguito il testo dell'esercizio proposto.


\clearpage
\thispagestyle{empty}
\subsection{Esercitazione I}

In questa esercitazione di Controlli Automatici si affrontano e si mettono in pratica i concetti fondamentali per l’analisi e la simulazione della risposta dei sistemi dinamici di natura meccanica ed elettrica.

La prima parte del lavoro è dedicata alla costruzione del modello, partendo dalle equazioni differenziali che governano il sistema (ad esempio leggi di Newton per sistemi massa–molla–smorzatore o leggi di Kirchhoff per circuiti RLC). Tramite la trasformata di Laplace, tali equazioni vengono trasformate in relazioni algebriche, rendendo possibile il calcolo della funzione di trasferimento ($G(s)$), strumento centrale per rappresentare il sistema nel dominio di Laplace e studiarne poli, zeri, stabilità e dinamica.

Successivamente si procede con il calcolo analitico delle risposte nel tempo, determinando l’andamento di grandezze come posizione, velocità, corrente o tensione a fronte di ingressi tipici (gradino, impulso, rampa). L’inversione della trasformata di Laplace (anche mediante scomposizione in fratti semplici) permette di ottenere le espressioni esplicite della risposta temporale, distinguendo tra transitorio e regime permanente e valutando indici prestazionali (tempo di assestamento, sovraelongazione, errore a regime).

Infine, i risultati teorici vengono confrontati e verificati tramite simulazione (ad esempio con strumenti come \textsc{Matlab}/\textsc{Simulink} o ambienti equivalenti), evidenziando la coerenza tra modello analitico e comportamento simulato. Questo approccio integrato consente di consolidare i metodi di modellazione e analisi dei sistemi lineari tempo-invarianti, fornendo una base solida per lo studio e la progettazione dei sistemi di controllo.


\vspace*{1cm}
\subsubsection{Simulazione della risposta di un sistema meccanico}
\label{sim1}

Si consideri il seguente sistema (\autoref{fig:E101}) dinamico meccanico SISO LTI a tempo continuo:

\begin{figure}[h]
\begin{center}

\begin{tikzpicture}[transform shape]
	\node[shape=rectangle, draw, line width=1pt, minimum width=1.215cm, minimum height=1.215cm] at (18.25, -14){};
	\node[shape=rectangle, minimum width=0.965cm, minimum height=0.84cm] at (18.25, -14){} node[anchor=center, align=center, text width=0.577cm, inner sep=6pt] at (18.25, -14){$M$};
	\draw[line width=0.8pt, -latex] (17.625, -14) -- (16.75, -14);
	\draw[line width=0.8pt] (18.875, -14) -- (19.375, -14);
	\draw[line width=0.8pt] (20.125, -14) -- (20.75, -14);
	\node[circ] at (20.75, -14){};
	\draw[line width=0.8pt] (19.5, -13.5) -- (19.875, -13.5) -- (19.875, -14.5) -- (19.5, -14.5);
	\draw[line width=0.8pt] (19.375, -14) -- (19.625, -14) -- (19.625, -13.75) -- (19.625, -14.25);
	\draw[line width=0.8pt] (20.375, -14) -- (19.875, -14);
	\draw (20.75, -14) to[spring] (22.75, -14);
	\draw[line width=0.8pt] (20.75, -14) -- (21.375, -14);
	\draw[line width=0.8pt] (22.125, -14) -- (22.75, -14);
	\node[shape=rectangle, fill={rgb,255:red,0;green,0;blue,0}, draw, line width=1pt, minimum width=0.715cm, minimum height=1.965cm] at (23.125, -14){};
	\draw[-latex] (23.875, -15) -- (16.75, -15);
	\draw (22.75, -15.375) -- (22.75, -12.625);
	\draw[dash pattern={on 0.4pt off 0.8pt}] (18.875, -14.625) -- (18.875, -15.375);
	\draw[dash pattern={on 0.4pt off 0.8pt}] (20.75, -14) -- (20.75, -15.375);
	\draw[-latex] (18.875, -15.375) -- (18, -15.375);
	\draw[-latex] (20.75, -15.375) -- (19.875, -15.375);
	\node[shape=rectangle, minimum width=0.715cm, minimum height=0.715cm] at (16.625, -13.625){} node[anchor=north west, align=left, text width=0.327cm, inner sep=6pt] at (16.25, -13.25){$F$};
	\node[shape=rectangle, minimum width=0.715cm, minimum height=0.715cm] at (19.75, -13.125){} node[anchor=north west, align=left, text width=0.327cm, inner sep=6pt] at (19.375, -12.75){$\beta$};
	\node[shape=rectangle, minimum width=0.965cm, minimum height=0.84cm] at (20.75, -13.562){} node[anchor=center, align=center, text width=0.577cm, inner sep=6pt] at (20.75, -13.562){$A$};
	\node[shape=rectangle, minimum width=0.965cm, minimum height=0.84cm] at (21.75, -13.313){} node[anchor=center, align=center, text width=0.577cm, inner sep=6pt] at (21.75, -13.313){$K$};
	\node[shape=rectangle, minimum width=0.84cm, minimum height=0.465cm] at (18.437, -15.625){} node[anchor=center, align=center, text width=0.452cm, inner sep=6pt] at (18.437, -15.625){$p,v$};
	\node[shape=rectangle, minimum width=0.84cm, minimum height=0.465cm] at (20.375, -15.625){} node[anchor=center, align=center, text width=0.452cm, inner sep=6pt] at (20.375, -15.625){$p_A,v_A$};
\end{tikzpicture}

\caption{Sistema dinamico meccanico SISO LTI a tempo continuo}
\label{fig:E101}
	
\end{center}
\end{figure}


che, scegliendo come variabili di ingresso $u = [F]$, stato $x = [x_1, \; x_2]^T = [v, \; p_A]^T$  ed uscita $y = [v]$, ha la seguente rappresentazione in variabili di stato:

\begin{equation}
	\begin{cases}	
	\dot x_1 = - \dfrac{K}{M}x_2 + \dfrac{1}{M}u \\
	\dot x_2 = x_1 - \dfrac{K}{\beta} x_2 \\
	y = x_1		
	\end{cases}
\end{equation}  

dove $M$ é la massa del corpo puntiforme, $K$ é la costante di elasticità della molla, $\beta$ é il coefficiente di attrito viscoso dello smorzatore mentre $F$ é la forza applicata alla massa. Assumendo $M$ = 0.3 kg ed $F(t) = F_0 \cos( \omega t) N$, si simuli il comportamento del sistema nei seguenti casi:

\begin{enumerate}
	\item L'ingresso $F(t)$ é un gradino unitario ($F_0 = 1$N, $\omega_0 = 0$ rad/s), mentre i valori numerici degli altri parametri e dello stato iniziale sono:
	
	\begin{enumerate}[label=\alph*)]
		\item $\beta = 0.1$Ns/m, $K = 2$ N/m, $x(t=0) = x_0 = [0,\;0]^T$;
		\item $\beta = 0.01$Ns/m, $K = 2$ N/m, $x(t=0) = x_0 = [0,\;0]^T$;
		\item $\beta = 10$Ns/m, $K = 20$ N/m, $x(t=0) = x_0 = [0,\;0]^T$;
		\item $\beta = 0.1$Ns/m, $K = 2$ N/m, $x(t=0) = x_0 = [0,\;0.2]^T$.
	\end{enumerate}
	
	\item L'ingresso $F(t)$ é di tipo sinusoidale con ($F_0 = 1$N, $\omega_0 = 4$ rad/s), mentre i valori numerici degli altri parametri e dello stato iniziale sono:

	\begin{enumerate}[label=\alph*)]
		\item $\beta = 0.1$Ns/m, $K = 2$ N/m, $x(t=0) = x_0 = [0,\;0]^T$;
		\item $\beta = 0.01$Ns/m, $K = 2$ N/m, $x(t=0) = x_0 = [0,\;0]^T$;
		\item $\beta = 10$Ns/m, $K = 20$ N/m, $x(t=0) = x_0 = [0,\;0]^T$;
		\item $\beta = 0.1$Ns/m, $K = 2$ N/m, $x(t=0) = x_0 = [0,\;0.2]^T$.
	\end{enumerate} 	
\end{enumerate}

Tracciare i grafici degli andamenti dell'evoluzione temporale degli stati e dell'uscita.

\textbf{Nota:} si ricorda di prendere in considerazione l'utilizzo dei comandi \textsc{Matlab} \texttt{ss} e \texttt{lsim}.

 % begin{enumerate}[label=\alph*)]
 
\subsubsection*{Soluzione esercizio \ref{sim1}}


\vspace*{1cm}
\subsubsection{Simulazione della risposta di un sistema elettrico}
\label{sim2}

Si consideri il seguente sistema dinamico elettrico SISO LTI a tempo continuo:

\begin{figure}[h]
\begin{center}
	
\begin{tikzpicture}[transform shape]
	\draw (17, -15) to[european current source, i=$i$, current/distance=0.3] (17, -11.75);
	\draw (19, -15) to[capacitor, v>=$v_C$, voltage/distance from node=0, voltage/bump b=1.1, voltage/shift=1.2] (19, -11.75);
	\draw (21, -11.75) to[american resistor, l={$R$}, i=$i_L$, current/distance=1] (21, -13.5);
	\draw (21, -13.5) to[cute inductor, l={$L$}] (21, -15);
	\draw (17, -11.75) to[short] (21, -11.75);
	\draw (17, -15) to[short] (21, -15);
\end{tikzpicture}
	
\caption{Sistema dinamico elettrico SISO LTI a tempo continuo}
\label{fig:E102}
	
\end{center}	
\end{figure}

che, scegliendo come variabili di ingresso $u = [i]$, stato $x = [x_1,\; x_2]^T = [v_C,\; i_L]^T$ ed uscita $y = [v_C]$, ha la seguente rappresentazione in variabili di stato:

\begin{equation}
\begin{cases}
	\dot x_1 = - \frac{1}{C} x_2 + \frac{1}{C} u \\
	\dot x_2 = - \frac{1}{L} x_1 - \frac{R}{L} x_2 \\
	y = x_1
\end{cases}	
\end{equation}

Assumendo $C = 0.2$ F ed $i(t) = i_0 \cos(\omega_0 t)$, si simuli il comportamento del sistema nei seguenti casi:

\begin{enumerate}

	\item L'ingresso $i(t)$ é un gradino unitario ($i_0 = 1$ A $\omega_0 = 0$ rad/s), mentre i valori numerici degli altri parametri e dello stato iniziale sono:
	\begin{enumerate}
	\item $R = 10\;\Omega$, $L = 0.5$ H, $x(t=0) = x_0 = [0,\;0]^T;$;
	\item $R = 100\;\Omega$, $L = 0.5$ H, $x(t=0) = x_0 = [0,\;0]^T;$;
	\item $R = 0.1\;\Omega$, $L = 0.05$ H, $x(t=0) = x_0 = [0,\;0]^T;$;
	\item $R = 10\;\Omega$, $L = 0.5$ H, $x(t=0) = x_0 = [0,\;0.2]^T;$;
	\end{enumerate}
	
	\item L'ingresso $i(t)$ é di tipo sinusoidale ($i_0 = 1$ A $\omega_0 = 4$ rad/s), mentre i valori numerici degli altri parametri e dello stato iniziale sono:
	\begin{enumerate}
	\item $R = 10\;\Omega$, $L = 0.5$ H, $x(t=0) = x_0 = [0,\;0]^T;$;
	\item $R = 100\;\Omega$, $L = 0.5$ H, $x(t=0) = x_0 = [0,\;0]^T;$;
	\item $R = 0.1\;\Omega$, $L = 0.05$ H, $x(t=0) = x_0 = [0,\;0]^T;$;
	\item $R = 10\;\Omega$, $L = 0.5$ H, $x(t=0) = x_0 = [0,\;0.2]^T;$;
	\end{enumerate}

\end{enumerate}

Tracciare i grafici degli andamenti dell'evoluzione temporale degli stati e dell'uscita.

\textbf{Nota:} si ricorda di prendere in considerazione l'utilizzo dei comandi \textsc{Matlab} \texttt{ss} e \texttt{lsim}.

\subsection*{Comandi \textsc{Matlab} \texttt{ss} e \texttt{lsim}}

Riportiamo ora brevemente un piccolo specchietto per ricordare le principali funzionalità dei comandi che si dovranno utilizzare. Si ricorda che é possibile una visione completa della documentazione online oppure digitando il comando \texttt{help} in ambiente di sviluppo.\\

Utilizzare \texttt{ss} per creare modelli stato-spazio a valore reale o a valore complesso oppure per convertire modelli di sistemi dinamici nella forma di modello stato-spazio.

Un modello stato-spazio è una rappresentazione matematica di un sistema fisico come un insieme di variabili di input, di output e di stato correlate da equazioni differenziali del primo ordine. Le variabili di stato definiscono i valori delle variabili di output. L'oggetto del modello \texttt{ss} può rappresentare modelli stato-spazio SISO o MIMO a tempo continuo o a tempo discreto.

A tempo continuo, un modello stato-spazio si presenta nella forma seguente:

\begin{equation}
\begin{cases}
	\dot x = Ax + Bu \\
	y = Cx + Du \\ 
\end{cases}	
\end{equation}

In questo caso, $x$, $u$ e $y$ rappresentano rispettivamente gli stati, gli input e gli output, mentre $A$, $B$, $C$ e $D$ sono le matrici stato-spazio. A tempo discreto, un modello stato-spazio assume la forma:

\begin{equation}
\begin{cases}
	x[n+1] = Ax[n] + Bu[n] \\
	y[n] = Cx[n] + Du[n] \\ 
\end{cases}	
\end{equation}

L'oggetto \texttt{ss} rappresenta un modello stato-spazio a tempo continuo o a tempo discreto in \textsc{Matlab} memorizzando $A$, $B$, $C$ e $D$ insieme ad altre informazioni, come il tempo di campionamento, i nomi di I/O, i ritardi e gli offset.\\

É dunque possibile creare un modello stato-spazio nei seguenti modi:

\lstinputlisting[language = Matlab, firstline = 16, lastline = 20]{code/ex.m}

Tutti i comandi sopra indicati possono essere seguiti da alcune proprità. Digitare \texttt{help ltiprops} per maggiori dettagli sulle proprietà assegnabili. L'impostazione \texttt{D = 0} viene interpretata come la matrice zero di dimensione adeguata. L'output \texttt{SYS} è un oggetto \texttt{SS}.


\lstinputlisting[language = Matlab, firstline = 22, lastline = 22]{code/ex.m}

Passando oltre il comando:

\lstinputlisting[language = Matlab, firstline = 26, lastline = 26]{code/ex.m}

permette di eseguire il plot della risposta temporale del sistema LTI modellato da \texttt{SYS} in base ai segnali di ingresso descritti da \texttt{T} ed \texttt{U}. Il vettore dei tempi \texttt{T} consiste di intervalli numerici equamente spaziati tra di loro mentre \texttt{U} é una matrice con tante colonne quanti input sono presenti. In particolare la \texttt{i}-esima riga specifica il valore di input al tempo \texttt{T(i)}. Per esempio:

 \lstinputlisting[language = Matlab, firstline = 28, lastline = 30]{code/ex.m}

simula la risposta di \texttt{SYS} ad $u(t) = \sin(t)$ durante un tempo di 5 secondi.
Si nota come nel tempo discreto \texttt{U} debba essere campionato con lo stesso passo con cui viene campionato il sistema (\texttt{T} diventa dunque ridondante e può essere omesso o dichiarato come matrice vuota).
Invece nel tempo continuo, il periodo di campionamento \texttt{T(2)-T(1)} deve essere piccolo abbastanza da catturare i dettagli del segnale in ingresso. Il vettore dei tempi \texttt{T} viene ricampionato quando potrebbero verificarsi oscillazioni tra i campioni.\\

Il comando:

 \lstinputlisting[language = Matlab, firstline = 32, lastline = 32]{code/ex.m}
 
 serve per specificare stati iniziali non nulli \texttt{XO} (solo per sistemi SS) mentre il comando:
 
 \lstinputlisting[language = Matlab, firstline = 34, lastline = 34]{code/ex.m}

serve per simulare la risposta di più sistemi LTI come \textt{SYS1} o \textt{SYS2} su di un singola operazione di plot. 
Notiamo come le condizioni iniziali \textt{XO} siano opzionali e che possa essere utile utilizzare diversi colori per distinguere i risultati ottenuti come indicato di seguito:


\lstinputlisting[language = Matlab, firstline = 36, lastline = 36]{code/ex.m}

Infine quanto il comando viene invocato fornendo degli argomenti a sinistra del simbolo dell'uguale:

\lstinputlisting[language = Matlab, firstline = 38, lastline = 38]{code/ex.m}

vengono ritornati la cronologia di output \texttt{Y} ed il vettore dei tempi \texttt{T} usati per la simulazione. In questo caso non viene mostrato nessun plot a schermo. Infine la matrice \texttt{Y} ha un numero di righe pari a \textt{LENGTH(T)} e tante colonne quanti sono gli output di \texttt{SYS}.

Infine per sistemi di tipo SS:

\lstinputlisting[language = Matlab, firstline = 42, lastline = 42]{code/ex.m}

viene anche ritornato la traiettoria dello stato \texttt{X} una matrice con un numero di righe pari a \textt{LENGTH(T)} e tante colonne quanti gli stati.


\subsubsection*{Soluzione esercizio \ref{sim2}}

\vspace*{1cm}
\subsubsection{Calcolo delle funzioni di trasferimento}
\label{sim3}

\subsubsection*{Soluzione esercizio \ref{sim3}}

\vspace*{1cm}
\subsubsection{Calcolo analitico di risposte nel tempo di sistemi dinamici mediante antitrasformata di Laplace}
\label{sim4}

\subsubsection*{Soluzione esercizio \ref{sim4}}

\end{document}